# 机器人数据基础设施报告

两个视角使用同一版本 `model.js`（2.1.0），不维护两套互相矛盾的单价和费用函数。

| 入口 | 面向对象 | 口径 |
|---|---|---|
| [index.html](index.html) | 技术、财务、采购 | 按日库存推演，默认 36 个 30 天月份，现金与折旧分列 |
| [v2.html](v2.html) | 管理层 | 四个业务变量、三个固定方案；稳定规模月运营费与追加投资分列 |

没有 CDN、外部字体、后端或遥测。将目录完整保存后可使用；也可生成两个独立 HTML：

```sh
python3 build.py --out dist
# dist/index.html 和 dist/v2.html 可单独打开；放在一起可互相跳转。
```

## 本次修正

- 用每日均匀新增的容量时间积分计费，月末库存只用于容量显示和资源需求；历史流入不再被当前增速覆盖。
- 明确数据年龄与最终删除期，验证 `lagDays <= hotDays <= stdDays <= arcDays`。原始留存比例不再绕过热缓冲；原始、成品库存可有限保留。
- 用实际转层量和有来源/目的地的数据流计搬运。任务关闭不产生该任务的输入传输；任务产物汇集、成品迁往存储位置和客户交付分别计费。
- 为已有资产和每笔新增采购分别记账。折旧在规定年限结束，累计不超过账面金额；原值、现金流和折旧不混用。已有容量不再次采购。
- 补入最低保存期不足费用、低频读取、归档恢复与临时副本；同云交付免流量不代表免读取费用；云上寄盘计到本地的搬运。
- CPU 系数只代表完整 GPU 实例之外的独立核时；云和 IDC 都显式考虑有效 GPU 利用率。默认计算重试开销为 0，代表未估计，而非保证没有重试。
- GB/GiB 明确转换；缺失报价不当作 0。腾讯归档参考修为 0.033，补回百度，火山仅保留标准 0.099 算例，其余留空。
- 基线保存参数、价格、模型版本，在当前月份和成本口径重算。策略按钮不再重置业务量或报价。
- 删除不完整成本口径下的“毛利率”；安全文本渲染、数值校验、情景版本校验、移动端和打印视图。

## V2 的刻意简化

只有每日新增、原始保留期、月交付量、计算负荷四类主输入。原始数据全量保留；成品体量为原始的 20%，保留 30 天。计算按每采集小时 24 GB 的等价工作量映射详细版任务链，必须用实际数据校准。

A 为全云标准，B 为前 7 天标准、之后低频，C 为原始与计算在 IDC、成品在云端低频。B 的最低保存期不足费用与低频读取计入预算。没有深度归档、复杂调度、自动寻优或供应商综合评分。

V2 月费为现金运营费，**不含自建设备折旧**；追加投资仅覆盖存储和 GPU 服务器。不能只按月费判断最优，也不能把它乘 12 充当从零扩张的真实首年预算。已有闲置设备的历史购买款不再算增量投入。会前前提中可填写已有容量和关键预算单价。

## 资料与假设

资料快照：2026-09-24；查询日期不是报价生效日。所有默认业务、算力、IDC、网络、恢复、请求预算均需核验，不能把“默认值可调”当作数据已经可信。

- 阿里容量：[官方计费说明](https://help.aliyun.com/zh/oss/storage-fees)。0.12 为算例；其他值标预算/历史参考，不称企业牌价。
- 腾讯：[官方价格中心](https://buy.cloud.tencent.com/price/cos)；[容量计量说明](https://cloud.tencent.com/document/product/436/53482)。目标地域、冗余方式仍需确认。
- 华为：[产品展示价格](https://www.huaweicloud.com/product/obs.html)；[计费规则](https://support.huaweicloud.com/price-obs/obs_42_0009.html)。起价与受限公测不代表可批量采购。
- 百度：[大陆普通类型价目表](https://cloud.baidu.com/product-price/bos.html)。冷存储/归档映射两级冷层，不与其他厂商同名对齐。
- 火山：[计费概述](https://www.volcengine.com/docs/6349/78455)。标准为北京算例，其余未知不填中位数。

输入数据采用十进制 TB/PB。模型容量价格按可选的 GiB 或十进制 GB 转换；原始价表写 GB 不足以确定字节数。腾讯已明确二进制，其他默认按 GiB 演示的项目需要合同确认。网络预算默认十进制 GB，需与具体路径合同对齐。报价税费、最低承诺与特殊阶梯尚未标准化。

## GPU / 计算资源价格参考

资料快照 2026-09-24。两份 HTML 现在同时展示国内算力租赁市场和公有云完整实例的参考价格，包括 4090、A10、A100 40/80G、A800 80G、H100 80G、H200 141G。市场参考来自 SMM 与 Mysteel 2026-09-23 附近行情；公有云示例采用百度 GPU 云服务器官方价目表。

不同地区、单卡/整机、包月/按需以及实例内 CPU/内存配置不同，因此这些价格只用于采购量级与敏感性讨论。当前成本模型继续使用 8 元/单卡完整实例小时作为通用预算参数；没有真实 workload benchmark 前，不按 GPU 型号自动替换模型单价或推导性能。

## 计算边界（不是完整 TCO）

每天恒定到达率，每月 30 天，增长在月初变化。新增流的库存按日首/日末梯形积分；整天年龄阈值下对均匀流入是精确积分。初始原始数据按已留存库存处理，初始批次用统一年龄表达；交付副本不扣减源库存。

计算任务视为读取新原始数据的任务组，而不是完整 DAG。同地远端输入按同一有序样本子集复用一次；不满足该假设时应增加实际搬运。关闭所有算法不等于关闭独立的成品打包。成品延迟只移动输出时间，不模拟排队。处理历史存量、重跑和全量迁移另行评估。

初始库存视为已位于方案指定地点，因此切换部署是目标布局比较，不包含迁移这些现有数据的历史成本。路径单价要含所有按流量费用，与固定接入费不能重复报价。聚合链路数是共享链路的估计需求，不是已验证的拓扑或 SLA。恢复时间从交付窗口扣除；窗口不足显示不可行。

IDC 按本工作负载预留容量计算；采购余量是 `需求 × (1+余量)`，不是使用率水位。每月按最大所需容量整块采购，采购当月开始摊销；到期继续使用并计运营，不自动替换。独立 CPU 服务器、机房/网络建设、融资、灾备和人员不在硬件追加投资内。计费窗口之后尚未结算的最低保存义务也不包含在累计金额中。

原始对象请求、小对象膨胀、外部平台服务费仍以预算或明确排除项处理；不会声称已经复刻供应商完整账单。默认 GPU/CPU 价格与吞吐不构成任何型号性能承诺。

## 情景与安全

导出 JSON 和参数 URL 包含业务量与报价，Base64 不是加密。只在授权范围分发。导入须具有 `schema: "2.1.0"` 及正确 `kind`，详细版为 `detail`、V2 为 `executive`。旧版没有版本信息的 `#s=` 和旧基线不静默沿用，以免在新公式下误解旧配置。

## 测试

```sh
node --test tests/model.test.cjs
# 可选浏览器测试：已安装 playwright 与 Chromium 时
CHROMIUM_PATH=/usr/bin/chromium python3 tests/browser_smoke.py
```

覆盖容量时间、增长库存、转层时点、禁用任务、成品路径、方向费率、计量单位、折旧上限、已有资产、最短保存期、恢复副本、缺价、无效输入、快照和两版稳态对账。浏览器测试将本地依赖内联到页面并阻断所有网络，测试参数、基线、导入导出、文本注入安全、390px 布局和打印。该方式不验证实际 GitHub Pages 部署与浏览器导航策略。
